[story]
New baseline, bl1, on dev stream, poll on childs. Merge error on deliver

[expected]
[[build now]]
(a)The baseline is not found, build is not initiated. 
Build baseline is bl1 and rejected
Created baseline is null
Result is failed.


[configuration]
 * Stream
 * Component
 * Promotion level
 * Load modules
 * Polling
 * Create baselines
 * Name template
 * Make tag
 * Set description

