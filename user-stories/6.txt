[story]
New baseline, bl1, on dev stream, poll on childs. Deliver in progress

[expected]
[[build now]]
(a)The first deliver is cancelled, jobs succeeds
Build baseline bl1 is rejected
Created baseline is valid
Result is SUCCESS


[configuration]
 * Stream
 * Component
 * Promotion level
 * Load modules
 * Polling
 * Create baselines
 * Name template
 * Make tag
 * Set description
 * Force deliver = true

